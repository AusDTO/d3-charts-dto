import { expect as expect } from 'chai';
import d3 from 'd3';
import Layer from 'Charts/Layer';

module.exports = function(callback) {
  describe(' ', () => {
    before(()=>{
      if(!callback) {

      }
       else {
      }
    });

    after(function() {
      chart.destroy();
    });
  });
};
